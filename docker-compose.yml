version: '2'
services:
  db:
    image: "postgres:9.6"
    ports:
      - "5436:5432"
  healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
